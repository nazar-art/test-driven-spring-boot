# configure auto-configured MultipartConfigElement

multipart:
  maxFileSize: 50MB
  maxRequestSize: 50MB
  llocation: ${java.io.tmpdir}

spring:
  mvc:
    favicon:
      enabled: false

logging:
  level.: debug

com:
  justgifit:
    optimize: true
    gif-location: ${java.io.tmpdir}/gif
